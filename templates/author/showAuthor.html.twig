<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Author Profile</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

<div class="container mt-5">
    <div class="card text-center shadow-sm">
        <div class="card-header bg-secondary text-white">
            <h2>Author Profile</h2>
        </div>
        <img src="{{ author.picture }}" class="card-img-top rounded-circle mx-auto mt-3" alt="{{ author.username }}" style="width: 150px; height: 150px;">
        <div class="card-body">
            <h3 class="card-title">{{ author.username }}</h3>
            <p class="card-text">Email: <a href="mailto:{{ author.email }}">{{ author.email }}</a></p>
            <p class="card-text">Number of books: <strong>{{ author.nb_books }}</strong></p>
            
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
