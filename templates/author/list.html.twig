<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>List of Authors</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>






<div class="container mt-5">
    <div class="text-center mb-4">
        <h2 class="text-primary">Meet Our Authors</h2>
        <p class="text-muted">Discover the most renowned authors and their works.</p>
    </div>


   


    {% if authors is empty %}
        <div class="alert alert-warning text-center">No authors available.</div>
    {% else %}
    <div class="row g-4">



 <div class="container mt-5">
    
    <div class="row justify-content-center">
        
        <div class="col-12 col-md-6 mb-4">
            <div class="card text-center border-0 shadow-sm">
                <div class="card-body">
                    <h4 class="card-title text-uppercase text-primary">Total Authors</h4>
                    <p class="display-4 fw-bold text-success"> {{ authors |length }}  </p>
                </div>
            </div>
        </div>

        
        <div class="col-12 col-md-6 mb-4">
            <div class="card text-center border-0 shadow-sm">
                <div class="card-body">
                    <h4 class="card-title text-uppercase text-primary">Total Books</h4>
                    <p class="display-4 fw-bold text-info"> 
                        {% set total_books = 0 %}
                            {% for author in authors %}
                                {% set total_books = total_books + author.nb_books %}
                            {% endfor %}
                        {{ total_books }}
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>







        {% for author in authors %}

        <div class="col-12">
            
            <div class="card h-100 border-0 shadow-sm hover-shadow">
                <div class="card-body d-flex align-items-center">
                    
                    <img src="{{ author.picture }}" alt="{{ author.username }}" class="rounded-circle me-3" style="width: 100px; height: 100px;">
                    
                    
                    <div class="flex-grow-1">
                        <h5 class="card-title text-uppercase mb-1">{{ author.username |upper }}</h5>
                        <p class="card-text text-muted mb-1">{{ author.email }}</p>
                        <span class="badge bg-success fs-6">{{ author.nb_books }} Books</span>
                    </div>
                    
                    <a href="/author/{{ author.id }} " class="btn btn-primary btn-sm ms-auto">View Profile</a>
                </div>
            </div>
        </div>
        {% endfor %}

    </div>
    {% endif %}
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
